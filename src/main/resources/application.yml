spring:
  profiles:
    active: dev
  datasource:
    url: "jdbc:mysql://ip:port/test"
    username: "user"
    password: "pass"
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8081
  context-path: /spring-boot-demo
